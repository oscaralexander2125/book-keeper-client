import React from 'react';

export default function About() {
  const text = 'Keep track of all your books and look for recommendations from other readers';
  return (
    <div className='about-app app-mobile'>
      {text}
    </div>
  )
}